# Définition de la liste L
L = [8, 24, 27, 48, 2, 16, 9, 102, 7, 84, 91]

# Récupération de la valeur, du maximum et du minimum
valeur = L[3]  # Choisissez n'importe quelle position dans la liste
valeur_max = max(L)
valeur_min = min(L)

# Affichage des résultats
print(f"La valeur est : {valeur}")
print(f"La valeur max est : {valeur_max}")
print(f"La valeur min est : {valeur_min}")